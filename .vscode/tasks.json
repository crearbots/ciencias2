{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run JavaFX with Maven",
            "type": "shell",
            "command": "mvn",
            "args": [
                "clean",
                "javafx:run"
            ],
            "options": {
                "cwd": "${workspaceFolder}/demo"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "Tarea para ejecutar JavaFX con Maven desde la carpeta 'demo'"
        }
    ]
}
